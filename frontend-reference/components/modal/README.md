# Modal

A modal interrupts the current task, so use it sparingly. A complete modal needs focus management and keyboard behavior; begin with semantic structure and study [MDN `<dialog>`](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/dialog) before adding JavaScript.
